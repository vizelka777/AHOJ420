# 🛡️ Project Ahoj420: Passkey-Only Identity Provider (2026) просто «форма входа», а полноценный Центр Управления Личностью. В 2026 году пользователи ожидают, что безопасность будет невидимой, а доступ — мгновенным.Вот подробное описание функциональных возможностей твоей системы:1. Регистрация: "Проще некуда"Никаких требований к сложности пароля, подтверждения пароля или капчи.Идентификация: Пользователь вводит только свой Email (или никнейм).Создание ключа: Браузер запрашивает биометрию (FaceID, TouchID, Windows Hello) или аппаратный ключ (Yubikey).Прогрессивное создание профиля: После подтверждения биометрии аккаунт создан. Публичный ключ привязан к Email в базе Postgres.2. Вход: "Магия Resident Keys"Мы используем технологию Discoverable Credentials, что дает уникальный UX:Вход в один клик: Если пользователь заходит с уже известного устройства, ему не нужно вводить даже Email. Система сама предлагает: "Войти как [Имя]?".Автозаполнение (Conditional UI): При клике на поле ввода логина браузер сразу предлагает доступные пасскеи в выпадающем списке.3. Кросс-девайс и QR-вход (The Bridge)Решение проблемы «чужого компьютера» без передачи паролей:Hybrid Flow: Если сайт открыт на чужом ПК, система показывает QR-код.Связь по Bluetooth: Пользователь сканирует код телефоном. Телефон и ПК обмениваются данными по зашифрованному каналу ближнего действия (протокол caBLE).Результат: Доступ разрешен на ПК, но секретный ключ физически никогда не покидает телефон.4. Управление устройствами (Multi-device)Личный кабинет, где пользователь — хозяин своих ключей:Объединение: Кнопка «Добавить устройство». Можно привязать рабочий ноутбук, личный телефон и запасной USB-ключ к одному Email.Идентификация девайсов: Благодаря AAGUID, система показывает: "Вход выполнен с MacBook Pro" или "Добавлен Google Pixel 9", а не просто "Устройство #1".Отзыв доступа: Если телефон потерян, пользователь может зайти с ноутбука и одним кликом удалить ключ потерянного телефона из базы.5. Восстановление: "План Б"Поскольку паролей нет, почта становится единственным «якорем»:Magic Link: Если все устройства потеряны, на почту отправляется одноразовая защищенная ссылка.Сессионный вход: Переход по ссылке создает временную сессию в Redis.Принудительный Setup: После такого входа система не пускает пользователя дальше страницы создания нового Passkey, чтобы он не остался без защиты.6. Роль провайдера (OIDC / SSO)Твой сайт становится «ключом» для других сайтов:OAuth2/OIDC Flow: Другие твои сайты (например, «Форум» или «CRM») не имеют своей базы пользователей. Они просто перенаправляют на Ahoj420.Единый вход (SSO): Залогинившись один раз в Ahoj420, пользователь автоматически авторизован во всех «дружественных» сервисах.Сводная таблица функционалаФункцияКак это работаетПрофит для пользователяРегистрацияWebAuthn Ceremony2 секунды, без паролейВход на чужом ПКQR-код + BluetoothБезопасно даже в интернет-кафеСинхронизацияiCloud/Google KeychainПасскей сам «переезжает» на новый iPhone/AndroidMulti-factorВстроено в PasskeyБиометрия — это уже 2FA по умолчаниюRecoveryEmail TokenБезопасный возврат доступа


Этот проект представляет собой современную систему аутентификации, где **пароли отсутствуют как класс**. Мы строим экосистему, в которой безопасность обеспечивается биометрией (WebAuthn), а гибкость — протоколом OIDC.

## 🏗 Технологический стек (The "Lean & Mean" Stack)

* **Язык:** Go 1.24+ (Backend logic)
* **Фронтенд:** HTMX + Tailwind CSS (No-SPA подход)
* **База данных:** PostgreSQL 16+ (Ключи и пользователи)
* **Кэш/Сессии:** Redis (Челленджи WebAuthn и сессии)
* **Прокси:** Caddy (SSL/TLS терминация)
* **Библиотеки:** - `github.com/go-webauthn/webauthn` (Core Auth)
    - `github.com/zitadel/oidc/v3` (OIDC Provider)

---

## 🗺 Схема базы данных (PostgreSQL)

```sql
-- Таблица пользователей
CREATE TABLE users (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    email TEXT UNIQUE NOT NULL,
    created_at TIMESTAMPTZ DEFAULT NOW()
);

-- Таблица ключей (Устройства)
CREATE TABLE credentials (
    id BYTEA PRIMARY KEY,             -- CredentialID
    user_id UUID REFERENCES users(id) ON DELETE CASCADE,
    public_key BYTEA NOT NULL,        -- Публичный ключ устройства
    aaguid BYTEA,                     -- ID производителя (Apple, Google и т.д.)
    sign_count UINT32 DEFAULT 0,      -- Защита от клонирования
    device_name TEXT,                 -- Имя устройства (напр. "iPhone 15")
    created_at TIMESTAMPTZ DEFAULT NOW(),
    last_used_at TIMESTAMPTZ
);

📂 Структура проекта
.
├── cmd/server/main.go       # Инициализация и запуск
├── internal/
│   ├── auth/                # WebAuthn (Begin/Finish flows)
│   ├── oidc/                # OIDC Storage & Discovery
│   ├── store/               # Работа с Postgres (Repository)
│   └── cache/               # Работа с Redis
├── web/
│   ├── static/js/glue.js    # JS-прослойка для navigator.credentials
│   └── templates/           # HTML шаблоны (HTMX компоненты)
├── Caddyfile                # Конфигурация HTTPS
├── docker-compose.yml       # Сборка всей системы
└── ahoj420.md               # Данная документация



🚀 План реализации (Roadmap)
Шаг 1: Окружение
Поднять docker-compose с Postgres и Redis.

Настроить Caddy для локального домена auth.localhost (WebAuthn требует HTTPS).

Шаг 2: Регистрация (Passkey First)
Реализовать GET /auth/register/begin (генерация Challenge в Redis).

Написать фронтенд-обработчик для вызова системного окна биометрии.

Реализовать POST /auth/register/finish (верификация и сохранение ключа).

Шаг 3: Cross-Device Login (QR)
Настроить AuthenticatorSelection в Go на ResidentKeyRequired.

Реализовать вход, который автоматически предлагает QR-код для чужих устройств.

Шаг 4: Восстановление (Magic Link)
Реализовать отправку токена на Email через Redis.

После входа по ссылке — принудительный редирект на регистрацию нового Passkey.

Шаг 5: OIDC (Identity Provider)
Реализовать эндпоинты /.well-known/openid-configuration.

Настроить выдачу ID Token для дружественных сайтов.

📝 Важные нюансы
AAGUID: Использовать метаданные для отображения иконок устройств.

Security: Caddy должен принудительно отдавать заголовки FIDO-RP-ID.

UX: Использовать WebAuthn Autofill (Conditional UI) для моментального входа.